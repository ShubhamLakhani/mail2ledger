/**
 * @file HDFC.gs
 * @description Parser module specifically designed for HDFC Bank transaction alert emails (debit/credit card, netbanking, UPI).
 */

function parseHDFCEmail(message) {
  Logger.log("Parsing HDFC email");
  return null;
}
